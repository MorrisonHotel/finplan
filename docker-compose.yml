services:
  app:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - finplan_node_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev"
    environment:
      - NODE_ENV=development

volumes:
  finplan_node_modules:
